<p style="color: green"><%= notice %></p>

<% content_for :title, "Attendance reports" %>

<h1 class="text-2xl font-bold">Attendance reports</h1>

<h2 class="text-xl font-semibold">Sunday Service</h2>

<div id="attendance_reports" class="mb-5">
  <% @sunday_reports.each do |attendance_report| %>
    
    <%= render attendance_report %>
  <% end %>
</div>

<%= link_to "New attendance report", new_attendance_report_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
